---
version: "3"

dotenv: ['k8s-on-macos.env']

tasks:
  default: 
    cmd: task --list --sort none
    silent: true

includes:
  machines:
    silent: true
    taskfile: taskfiles/machines.yaml

  kubernetes:
    silent: true
    taskfile: taskfiles/kubernetes.yaml

  addons:
    silent: true
    taskfile: taskfiles/addons.yaml

  brew:
    silent: true
    taskfile: taskfiles/homebrew.yaml
